<nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm sticky-top">
  <div class="container">
    <%= link_to root_path, class: "navbar-brand d-flex align-items-center gap-2" do %>
      <%= image_tag "AutoLink_Logo.png", alt: "AutoLink", height: 32, class: "d-inline-block" %>
    <% end %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMain" aria-controls="navbarMain" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarMain">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <%= link_to cars_path, class: 'nav-link d-flex align-items-center gap-1' do %>
          <i class="bi bi-car-front-fill"></i> <%= t('.cars') %>
        <% end %>
      </ul>

      <ul class="navbar-nav align-items-center gap-2">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="langDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            <i class="bi bi-globe-americas"></i>
            <%= I18n.locale == :'pt-BR' ? "Português" : "English" %>
          </a>
          <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="langDropdown">
            <li><%= link_to "Português", url_for(locale: 'pt-BR'), hreflang: 'pt-BR', rel: 'alternate', class: "dropdown-item" %></li>
            <li><%= link_to "English", url_for(locale: 'en'), hreflang: 'en', rel: 'alternate', class: "dropdown-item" %></li>
          </ul>
        </li>

        <li class="nav-item">
          <%= link_to "https://wa.me/5599999999999", target: "_blank", rel: "noopener", class: "btn btn-success btn-sm d-flex align-items-center gap-1" do %>
            <i class="bi bi-whatsapp"></i> <%= t('.contact_us') %>
          <% end %>
        </li>
      </ul>
    </div>
  </div>
</nav>
